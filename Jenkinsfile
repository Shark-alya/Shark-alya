pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'building application'
            }

        stage('Deploy') {
            steps {
                echo 'Переносим код в рабочую среду или создаем артефакт'
            }
        }
    }
}
